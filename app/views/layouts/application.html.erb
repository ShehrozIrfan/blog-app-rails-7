<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="prose mx-auto">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <% if user_signed_in? %>
      <%= link_to 'New Blog Post', new_blog_post_path, class: "bg-gray-400 px-4 rounded hover:bg-gray-600" %>
      <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: "bg-gray-100 px-4 py-2 rounded hover:bg-gray-200" %>
    <% end %>
    <%= yield %>
  </body>
</html>
